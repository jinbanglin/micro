syntax = "proto3";

package msg;

//传递的消息结构体
message Message {
    uint32 code = 1;
    string msg = 2;
    //    string session = 3;
}

//protoc --proto_path=${GOPATH}/src:. --gogofast_out=plugins=micro:. *.proto